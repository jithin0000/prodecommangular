import { DradDropDirective } from './drad-drop.directive';

describe('DradDropDirective', () => {
  it('should create an instance', () => {
    const directive = new DradDropDirective();
    expect(directive).toBeTruthy();
  });
});
